// Actual height of AppTopBar (h-14) and the bottom tab bar row (min-h-14) —
// used to inset full-bleed content (e.g. the map) so it renders in the gap
// between them instead of underneath.
export const APP_CHROME_INSET_REM = '3.5rem';
