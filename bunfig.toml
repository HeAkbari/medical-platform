# Bun configuration — mirrors easytrader workspace setup
# See: https://bun.sh/docs/runtime/bunfig

[install]
registry = "https://repo.mofid.dev/artifactory/api/npm/npm/"
linker = "hoisted"

[install.cache]
disable = false
disableManifest = false
